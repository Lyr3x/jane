<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <title>Jane</title>

    <!-- Lobster Font for Logo -->
    <!-- <link href='http://fonts.googleapis.com/css?family=Lobster' rel='stylesheet' type='text/css'> -->
    <!-- Bootstrap -->
    <link href="css/bootstrap.css" rel="stylesheet">
    <!-- iOS and Android icon -->
    <link rel="apple-touch-icon-precomposed" href="favicon-152.png">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>

  <body>
    <div class="container" style="margin-bottom: 60px">
        <!-- <h1 class="logo">Jane</h1> -->
        <div class="row">
            <div class="col-xs-10 col-md-4 col-sm-6 col-xs-offset-1 col-md-offset-0 col-sm-offset-0">  
                <div class="row">
                    <div class="container">
                        <% @i=0 %>
                        <%= render_ui(Jane.config)%>
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-10 col-md-12 col-sm-12 col-xs-offset-1 col-md-offset-0 col-sm-offset-0">  
                <h3>Timer</h3>
                <form class="form-inline" action="/timer" method="get">
                    <label for="device">Device</label>
                    <select class="form-control" id="devices">
                        <%= list_devices(Jane.config) %>
                    </select>
                    <label for="action">Action</label>
                    <select class="form-control" id="actions">
                        <%= list_actions(Jane.config) %>
                    </select>
                    <div class="form-group">
                        <label for="delay">Delay</label>
                        <input type="number" class="form-control" name="sec" placeholder="sec">
                        <input type="number" class="form-control" name="min" placeholder="min">
                        <input type="number" class="form-control" name="hour" placeholder="hours">
                    </div>
                    <button type="submit" class="btn btn-primary">set timer</button>
                </form>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-10 col-md-12 col-sm-12 col-xs-offset-1 col-md-offset-0 col-sm-offset-0">  
                <h4>Schedule</h4>
                <%= list_scheudled_jobs %>
            </div>
        </div>
    </div>
    <!-- call jane functions in background on button click -->
    <script>
    function jane(device_in, action_in){
        $.get("v1", { device : device_in, action : action_in});
    }
    </script>

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <!-- <script src="js/bootstrap.min.js"></script> -->
  </body>
</html>

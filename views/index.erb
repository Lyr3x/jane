<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <title>Jane</title>

    <!-- Lobster Font for Logo -->
    <link href='http://fonts.googleapis.com/css?family=Lobster' rel='stylesheet' type='text/css'>
    <!-- Bootstrap -->
    <link href="css/bootstrap.css" rel="stylesheet">
    <!-- iOS and Android icon -->
    <link rel="apple-touch-icon-precomposed" href="favicon-152.png">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>

  <body>
    <div class="container">
        <h1 class="logo">Jane</h1>
        <div class="row">
            <div class="col-xs-10 col-md-4 col-sm-6 col-xs-offset-1 col-md-offset-0 col-sm-offset-0">  
                <div class="row">
                    <div class="container">
                        <% @i=0 %>
                        <%= render_ui(Jane.config)%>
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
                <h4>Timer</h4>
                    <form class="form-inline" action="localhost:4567/timer" method="get">
                        <div class="form-group">
                            <label for="device">Device</label>
                            <input type="text" class="form-control" name="device" placeholder="tv">
                        </div>
                        <div class="form-group">
                            <label for="action">Action</label>
                            <input type="text" class="form-control" name="action" placeholder="on">
                        </div>
                        <div class="form-group">
                            <label for="delay">Delay</label>
                            <input type="number" class="form-control" name="time" placeholder="1">
                            <label class="radio-inline">
                              <input type="radio" name="mod" id="sec" value="s"> s
                            </label>
                            <label class="radio-inline">
                              <input type="radio" name="mod" id="min" value="m"> m
                            </label>
                            <label class="radio-inline">
                              <input type="radio" name="mod" id="hou" value="h"> h
                            </label>
                        </div>
                        <button type="submit" class="btn btn-default">set timer</button>
                    </form>
                </div>
    </div>
    
    <!-- call jane functions in background on button click -->
    <script>
    function jane(device_in, action_in){
        $.get("v1", { device : device_in, action : action_in});
    }
    </script>

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <!-- <script src="js/bootstrap.min.js"></script> -->
  </body>
</html>
